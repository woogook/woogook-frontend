# Common Entry

## 목적

- 서비스 도메인에 직접 속하지 않는 공통 작업에서 추가로 읽어야 할 문서를 지정한다.

## 추가 문서

- `docs/common/canonical/agent-control-plane-v2-cutover-map.md`
- 현재 작업과 직접 관련된 `docs/common/**` 문서

## 주의사항

- 공통 workflow는 `.agents/workflows/*.md`를 따른다.
- 문서, CI, GitHub template, agent helper, 공용 설정 변경은 기본적으로 `common`으로 본다.
