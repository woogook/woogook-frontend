# 이슈 작성 가이드

## 언제 읽는지

- 새 작업을 시작할 때
- 기존 Issue를 이어갈지 새로 만들지 판단할 때

## 기준

- 같은 목표와 범위의 open Issue가 있으면 재사용을 우선한다.
- 새 이슈에는 `배경`, `목표`, `범위`, `비범위`, `검증 계획`, `문서 영향`을 반드시 적는다.
- 관련 문서 경로와 핵심 작업 항목 체크리스트를 포함한다.
- 상태 변경은 `gh issue create`, `gh issue edit`, 필요 시 `gh api`를 사용한다.
